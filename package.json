{
  "name": "react-p",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "bootstrap": "^5.3.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-https-redirect": "^1.1.0",
    "react-router-dom": "^6.20.1",
    "react-scripts": "5.0.1",
    "three": "^0.159.0",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:window": "SET HTTPS=true&& set PORT=443 && set SSL_CRT_FILE=ancrypt/cert.pem&& set SSL_KEY_FILE=ancrypt/privkey.pem&& react-scripts start",
    "build": "SET HTTPS=true&& set PORT=443 && set SSL_CRT_FILE=ancrypt/cert.pem&& set SSL_KEY_FILE=ancrypt/privkey.pem&& react-scripts build",
    "serve": "serve -s build -l 443 --ssl-cert ancrypt/cert.pem --ssl-key ancrypt/privkey.pem"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "main": "src/cjs/index.js",
  "author": "",
  "license": "ISC"
}
